<?xml version="1.0" encoding="utf-8"?>
<!-- nav bar -->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/m_nav_drawer">

    <!-- Main Content -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/margin_large"
            android:background="@color/colorPrimary500">

            <ListView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:divider="@android:color/transparent"
                android:dividerHeight="@dimen/margin_large"
                android:id="@+id/m_lv_main_content">

            </ListView>

        </LinearLayout>

    <!-- Nav Layout -->
    <LinearLayout
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Bottom of nav -->
        <ListView
            android:layout_width="@dimen/navbar_width"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:choiceMode="singleChoice"
            android:divider="@android:color/transparent"
            android:dividerHeight="0dp"
            android:background="@color/colorPrimary100">

        </ListView>

        <!-- Top of nav -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary100"
            android:id="@+id/m_profile_box">
            <ImageView
                android:layout_width="@dimen/profile_diameter"
                android:layout_height="@dimen/profile_diameter"
                android:background="@drawable/circle"
                android:src="@drawable/ic_account_circle_100dp"/>
        </FrameLayout>

    </LinearLayout>

</android.support.v4.widget.DrawerLayout>
